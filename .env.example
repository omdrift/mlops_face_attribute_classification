# Environment variables for Face Attribute Classification API

# Data directory path (mounted from host)
DATA_DIR=/app/data

# Optional: Path to trained model weights
# If not provided or file doesn't exist, uses base ResNet-18 model
MODEL_PATH=/app/data/model.pth

# Maximum number of images to process on-the-fly (without annotations.csv)
# Lower values = faster but fewer results
# Higher values = more thorough but slower
MAX_IMAGES_TO_PROCESS=100

# Python settings
PYTHONUNBUFFERED=1
