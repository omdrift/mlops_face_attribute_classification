# Airflow Configuration
# ======================
# This file is used to configure Apache Airflow for this MLOps project

# User credentials
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# User ID for Airflow (important for file permissions)
# On Linux: run `echo $(id -u)` to get your user ID
AIRFLOW_UID=50000

# Project directory inside the Airflow container
PROJECT_DIR=/opt/airflow/project

# DVC Remote (uncomment and configure if using remote storage)
# DVC_REMOTE_URL=s3://my-bucket/dvc-storage
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=us-east-1

# MLflow Configuration (if using MLflow tracking)
# MLFLOW_TRACKING_URI=http://localhost:5000

# Model Performance Threshold
ACCURACY_THRESHOLD=0.85

# Email notifications (configure SMTP if needed)
# AIRFLOW__SMTP__SMTP_HOST=smtp.gmail.com
# AIRFLOW__SMTP__SMTP_PORT=587
# AIRFLOW__SMTP__SMTP_USER=your_email@gmail.com
# AIRFLOW__SMTP__SMTP_PASSWORD=your_password
# AIRFLOW__SMTP__SMTP_MAIL_FROM=your_email@gmail.com
